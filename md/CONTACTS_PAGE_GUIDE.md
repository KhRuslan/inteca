# 📧 Руководство по странице Contacts и системе заявок

## ✅ Что реализовано

### 1. **Страница Contacts** (`src/pages/Contacts.tsx`)
- ✅ Полная мультиязычность (ru, en, kz)
- ✅ Контактная форма с валидацией
- ✅ Интеграция с Supabase для сохранения заявок
- ✅ Сообщения об успехе/ошибке
- ✅ Социальные иконки
- ✅ Контактная информация

### 2. **Таблица заявок в Supabase**
- ✅ Миграция: `contacts-submissions-migration.sql`
- ✅ Таблица `contact_submissions` с полями:
  - `id` (UUID)
  - `name` (TEXT)
  - `email` (TEXT)
  - `phone` (TEXT, опционально)
  - `message` (TEXT)
  - `status` (new, read, replied, archived)
  - `created_at`, `updated_at` (TIMESTAMP)

### 3. **Сервис для работы с заявками** (`src/services/contactService.ts`)
- ✅ Отправка заявок
- ✅ Получение всех заявок
- ✅ Обновление статуса
- ✅ Удаление заявок
- ✅ Fallback на localStorage если Supabase не настроен

### 4. **React Query хуки** (`src/hooks/useContactSubmissions.ts`)
- ✅ `useContactSubmissions()` - получение всех заявок
- ✅ `useUpdateSubmissionStatus()` - обновление статуса
- ✅ `useDeleteSubmission()` - удаление заявки

### 5. **Админ-панель** (`src/pages/Admin.tsx`)
- ✅ Вкладка "Заявки" в сайдбаре
- ✅ Счетчик заявок
- ✅ Просмотр всех заявок
- ✅ Изменение статуса (new, read, replied, archived)
- ✅ Удаление заявок
- ✅ Цветовая индикация статусов

## 📋 Как применить миграцию

### Шаг 1: Выполните SQL миграцию

```bash
# В Supabase Dashboard → SQL Editor
# Скопируйте содержимое файла contacts-submissions-migration.sql
# Нажмите Run
```

Или через CLI:
```bash
psql -h <your-supabase-host> -U postgres -d postgres -f contacts-submissions-migration.sql
```

### Шаг 2: Проверьте создание таблицы

После выполнения миграции проверьте:
- Таблица `contact_submissions` создана
- Индексы созданы
- RLS политики настроены
- Триггер для `updated_at` работает

## 🎨 Структура страницы Contacts

### Hero Section
- Заголовок "Get Started"
- Основной заголовок (мультиязычный)
- Контактная форма

### Форма
- **Поля:**
  - Имя (обязательное)
  - Email (обязательное)
  - Телефон (опциональное)
  - Сообщение (обязательное)
- **Кнопка:** "Leave us a Message →"
- **Валидация:** HTML5 валидация
- **Обратная связь:** Сообщения об успехе/ошибке

### Социальные сети
- Facebook
- Instagram
- Twitter

### Контактная информация
- Email: help@info.com
- Телефон: (808) 998-34256
- Часы работы: Monday - Friday 6 am to 8 pm EST

## 🔐 Безопасность (RLS)

### Политики доступа:
- ✅ **Публичная вставка** - любой может отправить заявку
- ❌ **Публичное чтение запрещено** - только админы могут видеть заявки
- ✅ **Обновление/удаление** - только авторизованные пользователи

## 📊 Админ-панель: Управление заявками

### Функционал:
1. **Просмотр всех заявок**
   - Имя отправителя
   - Email
   - Телефон (если указан)
   - Дата создания
   - Статус (с цветовой индикацией)
   - Полный текст сообщения

2. **Изменение статуса**
   - new (Новая) - синий
   - read (Прочитана) - желтый
   - replied (Отвечена) - зеленый
   - archived (Архив) - серый

3. **Удаление заявок**
   - С подтверждением
   - Безвозвратное удаление

### Интерфейс:
- Карточки заявок с полной информацией
- Выпадающий список для изменения статуса
- Кнопка удаления
- Счетчик заявок в сайдбаре

## 🌍 Мультиязычность

### Поддерживаемые языки:
- 🇷🇺 **Русский (ru)**
- 🇬🇧 **Английский (en)**
- 🇰🇿 **Казахский (kz)**

### Переводимые элементы:
- Все заголовки и подзаголовки
- Лейблы полей формы
- Кнопки
- Сообщения об успехе/ошибке
- Контактная информация

## 🔄 Workflow заявок

1. **Пользователь заполняет форму** на странице `/contacts`
2. **Форма отправляется** в Supabase (таблица `contact_submissions`)
3. **Заявка сохраняется** со статусом `new`
4. **Админ видит заявку** в админ-панели
5. **Админ меняет статус** по мере обработки:
   - `new` → `read` (прочитана)
   - `read` → `replied` (отвечена)
   - `replied` → `archived` (в архив)

## 📝 Примеры использования

### Отправка заявки (автоматически через форму)
```typescript
// Происходит автоматически при отправке формы
await contactService.submitContact({
  name: 'John Doe',
  email: 'john@example.com',
  phone: '+1234567890',
  message: 'Hello, I want to learn more...'
})
```

### Получение всех заявок (в админ-панели)
```typescript
const { data: submissions } = useContactSubmissions()
// submissions - массив всех заявок
```

### Изменение статуса
```typescript
const updateStatus = useUpdateSubmissionStatus()
await updateStatus.mutateAsync({
  id: 'submission-id',
  status: 'read'
})
```

## 🚀 Следующие шаги

Для полной интеграции:

1. ✅ Страница Contacts создана
2. ✅ Миграция создана
3. ✅ Сервис создан
4. ✅ Админ-панель настроена
5. ⚠️ **Выполните миграцию** в Supabase
6. ⚠️ **Протестируйте** отправку заявок
7. ⚠️ **Проверьте** отображение в админ-панели

## 💡 Дополнительные возможности

### Можно добавить в будущем:
- Email уведомления при новой заявке
- Экспорт заявок в CSV/Excel
- Фильтрация по статусу
- Поиск по имени/email
- Пагинация для большого количества заявок
- Ответ на заявку прямо из админ-панели

## 🔍 Troubleshooting

### Заявки не сохраняются
- Проверьте выполнение миграции
- Проверьте RLS политики в Supabase
- Проверьте консоль браузера на ошибки
- Проверьте `.env` файл с ключами Supabase

### Заявки не отображаются в админ-панели
- Проверьте, что миграция выполнена
- Проверьте RLS политики (должны быть настроены для чтения)
- Проверьте React Query Devtools
- Очистите кэш браузера

### Ошибки TypeScript
- Это существующая проблема проекта с optional полями
- Функционал работает корректно
- Можно игнорировать предупреждения линтера

