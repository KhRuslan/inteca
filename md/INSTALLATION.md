# 💻 Установка и настройка INTECA

## 📋 Предварительные требования

- Node.js >= 18.0.0
- npm >= 9.0.0
- Git (для клонирования)
- Современный браузер (Chrome, Firefox, Safari, Edge)

---

## 🚀 Установка

### Шаг 1: Клонирование проекта (если нужно)

```bash
git clone <repository-url>
cd inteca
```

Или если проект уже у вас:
```bash
cd /Users/ruslan/Programming/inteca
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

Это установит:
- ✅ React 18
- ✅ TypeScript
- ✅ Vite
- ✅ Tailwind CSS
- ✅ React Router DOM
- ✅ Supabase Client
- ✅ React Query (TanStack Query)

**Время установки:** ~2-3 минуты

---

## ⚙️ Конфигурация

### Вариант 1: Без Supabase (быстрый старт)

Ничего настраивать не нужно! Проект работает из коробки с localStorage.

```bash
npm run dev
```

Откройте: `http://localhost:5173` ✅

### Вариант 2: С Supabase (рекомендуется)

#### 2.1. Создание проекта Supabase

1. Откройте [supabase.com](https://supabase.com)
2. Войдите или зарегистрируйтесь (бесплатно)
3. Нажмите **"New Project"**
4. Заполните:
   - Name: `inteca-website`
   - Database Password: создайте надежный пароль (сохраните!)
   - Region: выберите ближайший (Europe, Asia, Americas)
5. Нажмите **"Create new project"**
6. Подождите 1-2 минуты (проект создается)

#### 2.2. Настройка базы данных

1. В Supabase Dashboard откройте **SQL Editor**
2. Нажмите **"New query"**
3. Откройте файл `supabase-schema.sql` в вашем проекте
4. Скопируйте ВСЁ содержимое (Ctrl+A, Ctrl+C)
5. Вставьте в SQL Editor (Ctrl+V)
6. Нажмите **"Run"** (или F5) ▶️
7. Дождитесь "Success" ✅

**Проверка:**
- Откройте **Table Editor**
- Должны быть таблицы: `site_content` и `blog_posts`
- В `site_content` должна быть 1 строка

#### 2.3. Получение API ключей

1. Перейдите в **Settings** (шестеренка слева внизу)
2. Откройте **API**
3. Скопируйте:
   - **Project URL** (например: https://abc123.supabase.co)
   - **anon public** key (длинная строка начинается с eyJ...)

⚠️ **Важно:** НЕ копируйте `service_role` key!

#### 2.4. Создание .env файла

**Mac/Linux:**
```bash
cd /Users/ruslan/Programming/inteca
touch .env
nano .env
```

**Windows:**
```bash
cd /Users/ruslan/Programming/inteca
type nul > .env
notepad .env
```

**Содержимое .env:**
```env
VITE_SUPABASE_URL=https://ваш-проект-id.supabase.co
VITE_SUPABASE_ANON_KEY=eyJваш-anon-ключ-здесь
```

Замените на ВАШИ значения!

**Сохранение:**
- nano: `Ctrl+O`, `Enter`, `Ctrl+X`
- notepad: File → Save

#### 2.5. Проверка конфигурации

```bash
# Проверьте что .env создан
ls -la | grep .env

# Должны увидеть:
# .env

# Проверьте содержимое
cat .env

# Должно быть:
# VITE_SUPABASE_URL=https://...
# VITE_SUPABASE_ANON_KEY=eyJ...
```

---

## 🎮 Запуск проекта

### Development режим

```bash
npm run dev
```

**Вывод должен быть:**
```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
  ➜  press h to show help
```

Откройте браузер: `http://localhost:5173`

### Production build

```bash
npm run build
```

Проверка production сборки:
```bash
npm run preview
```

---

## ✅ Проверка установки

### Checklist:

Откройте сайт и проверьте:

- [ ] **Главная страница** отображается
  - Видны все секции
  - Изображения загружаются
  - Логотип отображается

- [ ] **Блог** (`/blog`) работает
  - Есть 3 примера статей (или ваши если добавили)
  - Поиск работает
  - Фильтр по тегам работает
  - Клик на статью открывает полную версию

- [ ] **Админ-панель** (`/admin`) доступна
  - Форма входа отображается
  - Пароль `admin123` работает
  - Видно меню секций слева
  - Можно редактировать контент

- [ ] **Supabase** подключен (если настроили)
  - Откройте DevTools (F12) → Console
  - Не должно быть ошибок "Supabase client not configured"
  - В Network tab должны быть запросы к Supabase

### Тест редактирования:

1. Откройте `/admin`
2. Войдите (admin123)
3. Выберите "Hero Section"
4. Измените заголовок
5. Нажмите "Сохранить"
6. Вернитесь на главную (`/`)
7. Проверьте - заголовок изменился? ✅

### Тест блога:

1. Откройте `/admin`
2. Выберите "Blog Posts"
3. Нажмите "Создать пост"
4. Заполните форму
5. Сохраните
6. Откройте `/blog`
7. Новый пост отображается? ✅

---

## 🐛 Troubleshooting

### Проблема: `npm install` не работает

**Решение:**
```bash
# Очистите кэш npm
npm cache clean --force

# Удалите node_modules
rm -rf node_modules package-lock.json

# Установите заново
npm install
```

### Проблема: Порт 5173 занят

**Решение:**
```bash
# Убейте процесс на порту
lsof -ti:5173 | xargs kill -9

# Или используйте другой порт
npm run dev -- --port 3000
```

### Проблема: "Module not found"

**Решение:**
```bash
# Проверьте что все зависимости установлены
npm install

# Если не помогло - очистите кэш Vite
rm -rf node_modules/.vite
npm run dev
```

### Проблема: Supabase не подключается

**Решение:**
1. Проверьте `.env` файл существует
2. Проверьте формат ключей (без пробелов, кавычек)
3. Проверьте URL начинается с `https://`
4. Перезапустите dev server (Ctrl+C, npm run dev)

**Проверка:**
```bash
# В DevTools Console выполните:
console.log(import.meta.env.VITE_SUPABASE_URL)
# Должен вывести ваш URL
```

### Проблема: Белый экран

**Решение:**
1. Откройте DevTools (F12)
2. Перейдите в Console
3. Найдите красные ошибки
4. Если "Cannot find module" - выполните `npm install`
5. Если синтаксические ошибки - проверьте последние изменения

### Проблема: TypeScript ошибки

**Решение:**
```bash
# Проверьте типы
npm run tsc --noEmit

# Если много ошибок - попробуйте
rm -rf node_modules
npm install
```

---

## 🔧 Дополнительная настройка

### Изменение порта

Файл `vite.config.ts`:
```typescript
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000  // Ваш порт
  }
})
```

### Настройка hot reload

Если hot reload не работает:
```typescript
// vite.config.ts
server: {
  watch: {
    usePolling: true  // Для некоторых систем
  }
}
```

### Proxy для API

Если нужен proxy:
```typescript
// vite.config.ts
server: {
  proxy: {
    '/api': 'http://localhost:8000'
  }
}
```

---

## 📦 Build для продакшена

### Production build

```bash
npm run build
```

**Результат:** Папка `dist/` с оптимизированным кодом

### Анализ bundle

```bash
# Установите анализатор
npm install -D rollup-plugin-visualizer

# В vite.config.ts добавьте
import { visualizer } from 'rollup-plugin-visualizer'

plugins: [
  react(),
  visualizer()
]

# Соберите
npm run build

# Откроется stats.html с визуализацией
```

### Deploy

**Vercel:**
```bash
npm i -g vercel
vercel
```

**Netlify:**
```bash
npm i -g netlify-cli
netlify deploy
```

**Manual (любой хостинг):**
```bash
npm run build
# Загрузите содержимое dist/ на сервер
```

---

## 🎯 Следующие шаги

После успешной установки:

1. **Кастомизация:**
   - Замените логотипы в `/public/`
   - Измените цвета в `tailwind.config.js`
   - Отредактируйте контент через `/admin`

2. **Добавление контента:**
   - Создайте посты в блоге
   - Загрузите свои изображения
   - Настройте тексты под свой бренд

3. **Продакшен:**
   - Настройте Supabase (если еще нет)
   - Измените пароль админки
   - Настройте домен
   - Deploy на хостинг

4. **Мониторинг:**
   - Подключите Google Analytics
   - Настройте Sentry для ошибок
   - Мониторьте Supabase Dashboard

---

## 📚 Дополнительные ресурсы

### Документация проекта:
- `START_HERE.md` - начните отсюда
- `QUICK_START.md` - быстрый старт
- `SUPABASE_SETUP.md` - настройка backend

### Внешняя документация:
- [React Query](https://tanstack.com/query/latest/docs/react/overview)
- [Supabase](https://supabase.com/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Vite](https://vitejs.dev/guide/)

---

## ✅ Готово!

Если установка прошла успешно, вы должны видеть работающий сайт на `http://localhost:5173`

**Проблемы?** Читайте секцию Troubleshooting выше или документацию проекта.

**Всё работает?** Отлично! Переходите к `ADMIN_GUIDE.md` для работы с админкой! 🎉

