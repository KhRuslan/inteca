# Руководство по странице Methodology & Cases

## Что было добавлено

### 1. Новые типы данных (`src/types/content.ts`)

Добавлены следующие интерфейсы:
- `MethodologyHero` - Hero секция страницы
- `MethodologyBenefit` - Преимущества методологии
- `MethodologyCase` - Кейсы по разным областям
- `MethodologyPageContent` - Общий интерфейс страницы

### 2. Админ-панель (`src/pages/Admin.tsx`)

Добавлена новая страница "Methodology & Cases" с тремя секциями:
- **Hero** - редактирование заголовка, описания и текста кнопки
- **Benefits** - редактирование 4 преимуществ методологии
- **Cases** - редактирование 7 кейсов по разным областям (Marketing, Economics, Strategy, Finance, Leadership, Sales, HR)

### 3. SQL Миграция (`methodology-content-migration.sql`)

Создана миграция для добавления контента на **3 языках**:
- 🇷🇺 **Русский (ru)**
- 🇬🇧 **Английский (en)**  
- 🇰🇿 **Казахский (kz)**

## Как применить миграцию

### Шаг 1: Подключитесь к базе данных Supabase

```bash
# Через Supabase CLI
supabase db reset

# Или через SQL Editor в Supabase Dashboard
```

### Шаг 2: Выполните миграцию

```bash
# Если используете Supabase CLI
supabase db push

# Или скопируйте содержимое файла methodology-content-migration.sql
# и выполните его в SQL Editor на dashboard.supabase.com
```

### Шаг 3: Проверьте результаты

После выполнения миграции SQL-запрос в конце файла покажет:
- Количество преимуществ (benefits_count: 4)
- Количество кейсов (cases_count: 7)
- Заголовки hero-секции для каждого языка

## Структура контента

### Hero Section
```typescript
{
  title: string        // Заголовок страницы
  description: string  // Описание под заголовком
  buttonText: string   // Текст кнопки CTA
}
```

### Benefits (4 элемента)
```typescript
[
  {
    title: string       // Заголовок преимущества
    description: string // Описание (2 строки)
  }
]
```

Включает:
1. Real Decision-Making
2. Structured Thinking First
3. Leadership Under Pressure
4. Global → Local

### Cases (7 элементов)
```typescript
[
  {
    title: string       // Название области
    description: string // Описание кейса
    linkText: string    // Текст ссылки
  }
]
```

Включает:
1. Marketing
2. Economics
3. Strategy
4. Finance
5. Leadership
6. Sales
7. Human Resource Management (HR)

## Редактирование контента

### Через Админ-панель

1. Перейдите на `/admin`
2. Войдите с паролем `admin123`
3. Выберите язык контента (🇷🇺 🇬🇧 🇰🇿)
4. Раскройте секцию "Methodology & Cases"
5. Выберите нужную подсекцию (Hero, Benefits, Cases)
6. Редактируйте контент
7. Изменения сохраняются автоматически при вводе

### Прямо в базе данных

```sql
-- Пример обновления заголовка Hero для русского языка
UPDATE site_content 
SET content = jsonb_set(
  content,
  '{methodologyPage,hero,title}',
  '"Новый заголовок"'
)
WHERE language = 'ru';
```

## Особенности

### Диаграмма (Donut Chart)
Диаграмма в hero-секции статичная и генерируется через SVG. Она показывает распределение:
- 🔴 Strategic Learning (красный)
- ⚪ Leadership Training (серый)
- ⚫ Advisory & Transformation (темно-серый)

### Иконки Benefits
Иконки для каждого преимущества закодированы в SVG и отображаются справа от текста:
- 🛡️ Real Decision-Making (щит)
- 🔲 Structured Thinking First (сетка)
- ❤️ Leadership Under Pressure (сердце)
- 🌍 Global → Local (глобус)

### Адаптивность
Страница полностью адаптивна и оптимизирована для:
- Мобильных устройств (< 640px)
- Планшетов (640px - 1024px)
- Десктопов (> 1024px)

## Проверка работы

1. Запустите проект: `npm run dev`
2. Откройте страницу: `http://localhost:5173/methodology-cases`
3. Переключайте языки через меню в шапке
4. Проверьте отображение всех секций

## Troubleshooting

### Контент не отображается
- Убедитесь, что миграция выполнена успешно
- Проверьте в Supabase Dashboard наличие данных в таблице `site_content`
- Проверьте консоль браузера на ошибки

### Ошибки TypeScript в Admin.tsx
- Это существующая проблема проекта с optional полями
- Функционал работает корректно, несмотря на предупреждения линтера
- При необходимости можно добавить дополнительные type guards

### Контент на одном языке не обновляется
- Убедитесь, что выбран правильный язык в админ-панели
- Проверьте, что изменения сохраняются в базе данных
- Очистите кэш браузера

## Следующие шаги

Для полной интеграции страницы:

1. ✅ Типы данных добавлены
2. ✅ Админ-панель настроена
3. ✅ Миграция создана
4. ⚠️ Страница уже создана (`src/pages/MethodologyCases.tsx`)
5. ✅ Маршрут уже должен быть в роутере

Проверьте файл роутинга (`src/App.tsx` или `src/router.tsx`), чтобы убедиться, что маршрут `/methodology-cases` добавлен.

