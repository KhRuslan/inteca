# 📱 Руководство по мобильной адаптации INTECA

## ✅ Что реализовано

Сайт полностью адаптирован для всех устройств с использованием mobile-first подхода.

---

## 🎯 Брейкпоинты Tailwind CSS

```
sm:  640px  - Мобильные (большие)
md:  768px  - Планшеты
lg:  1024px - Ноутбуки
xl:  1280px - Десктопы
2xl: 1536px - Большие экраны
```

---

## 📐 Адаптированные компоненты

### 1. **TopBar**

**Мобильные (<768px):**
- Скрыты навигационные ссылки
- Отображается логотип "INTECA"
- Языковые кнопки: 28px × 28px
- Уменьшенные отступы

**Десктоп (≥768px):**
- Все навигационные ссылки видны
- Языковые кнопки: 32px × 32px
- Полные отступы

```tsx
// Пример адаптации
<div className="hidden md:flex items-center gap-3 lg:gap-6">
  {/* Видно только на десктопе */}
</div>
```

### 2. **Header**

**Мобильные (<1024px):**
- Логотип: 80px → 96px высота
- Бургер-меню вместо навигации
- Мобильное меню раскрывается вниз
- Ссылки в колонку

**Десктоп (≥1024px):**
- Логотип: 128px высота
- Горизонтальная навигация
- Бургер скрыт

```tsx
// Бургер-меню
<button className="lg:hidden p-2">
  <svg className="w-6 h-6">...</svg>
</button>

// Десктопная навигация
<nav className="hidden lg:flex items-center gap-4 xl:gap-8">
  {/* Ссылки */}
</nav>
```

### 3. **HeroSection**

**Мобильные:**
- Высота: 300px
- Плашка: 95% ширины
- Заголовок: 16px → 20px → 24px
- Padding: 16px
- Bottom offset: -100px

**Десктоп:**
- Высота: 550px
- Плашка: 85% ширины
- Заголовок: 32px → 40px → 48px
- Padding: 48px
- Bottom offset: -160px

```tsx
// Адаптивная высота
className="h-[300px] sm:h-[400px] lg:h-[550px]"

// Адаптивный заголовок
className="text-base sm:text-xl md:text-2xl lg:text-3xl"
```

### 4. **ProgramBenefits**

**Мобильные:**
- 1 колонка
- Padding карточек: 16px
- Заголовки: text-lg

**Планшеты:**
- 2 колонки
- Padding: 24px

**Десктоп:**
- 3 колонки
- Полный padding

```tsx
// Адаптивная сетка
className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6"
```

### 5. **WhoProgramFor**

**Мобильные:**
- 1 колонка
- Иконки: 64px
- Padding: 24px

**Планшеты:**
- 2 колонки

**Десктоп:**
- 3 колонки
- Иконки: 80px
- Padding: 32px

### 6. **CaseBasedLearning**

**Мобильные:**
- Изображение сверху (order-2)
- Текст снизу (order-1)
- 1 колонка
- Уменьшенные заголовки

**Десктоп:**
- 2 колонки side-by-side
- Изображение слева
- Текст справа

```tsx
// Изменение порядка
<div className="order-2 lg:order-1">{/* Image */}</div>
<div className="order-1 lg:order-2">{/* Text */}</div>
```

### 7. **KeyBenefits**

**Мобильные:**
- Диаграмма: 192px × 192px
- 1 колонка (диаграмма + список)
- Уменьшенный текст и отступы
- Padding блока: 16px

**Десктоп:**
- Диаграмма: 320px × 320px
- 2 колонки
- Padding: 48px

```tsx
// Адаптивная диаграмма
className="w-48 h-48 sm:w-64 sm:h-64 lg:w-80 lg:h-80"
```

### 8. **Founder**

**Мобильные:**
- Изображение сверху
- Текст снизу
- 1 колонка
- Nameplate: меньше

**Десктоп:**
- 2 колонки
- Изображение слева
- Текст справа

### 9. **CTABanner**

**Мобильные:**
- Скрыты декоративные элементы
- Заголовок: text-xl
- Меньший padding
- Кнопка: меньше

**Десктоп:**
- Видны dots и волны
- Заголовок: text-5xl
- Полный padding

```tsx
// Скрытие декора на мобильных
<div className="hidden sm:block absolute top-8 left-8">
  {/* Декоративные dots */}
</div>
```

### 10. **Footer**

**Мобильные:**
- Логотип: 80px
- 1 колонка
- Меньший текст
- Padding: 32px

**Десктоп:**
- Логотип: 128px
- 2 колонки
- Нормальный текст
- Padding: 48px

### 11. **Blog (страница)**

**Hero Section:**
- Заголовок: 30px → 40px → 48px
- Меньший padding

**Search & Filter:**
- Вертикальный layout на мобильных
- Горизонтальный на десктопе
- Теги переносятся

**Featured Posts:**
- 1 колонка (мобильные)
- 2 колонки (десктоп)
- Высота карточек: 192px → 256px

**Regular Posts:**
- 1 колонка (мобильные)
- 2 колонки (планшеты)
- 3 колонки (десктоп)

```tsx
// Адаптивный поиск
<div className="flex flex-col md:flex-row gap-3 sm:gap-4">
  <input className="w-full md:w-80 lg:w-96" />
  <div className="flex flex-wrap gap-2">{/* Tags */}</div>
</div>
```

### 12. **BlogPost (страница)**

**Hero Image:**
- Высота: 250px → 350px → 400px
- Заголовок: text-2xl → text-5xl

**Контент:**
- Prose: prose-sm → prose-base → prose-lg
- Адаптивные размеры текста
- Padding: 32px → 64px

**Related Posts:**
- 1 колонка (мобильные)
- 2 колонки (планшеты)
- 3 колонки (десктоп)

---

## 🎨 Адаптивные паттерны

### Скрытие/Показ элементов

```tsx
// Скрыто на мобильных, видно на десктопе
className="hidden md:block"

// Видно на мобильных, скрыто на десктопе
className="md:hidden"
```

### Адаптивные сетки

```tsx
// От 1 до 3 колонок
className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3"

// От 1 до 2 колонок
className="grid grid-cols-1 lg:grid-cols-2"
```

### Адаптивные размеры текста

```tsx
// Заголовки
className="text-2xl sm:text-3xl lg:text-4xl xl:text-5xl"

// Обычный текст
className="text-sm sm:text-base lg:text-lg"

// Маленький текст
className="text-xs sm:text-sm"
```

### Адаптивные отступы

```tsx
// Padding
className="p-4 sm:p-6 lg:p-8"

// Margin
className="mb-4 sm:mb-6 lg:mb-8"

// Gap
className="gap-4 sm:gap-6 lg:gap-8"
```

### Адаптивные размеры

```tsx
// Ширина
className="w-full md:w-96"

// Высота
className="h-48 sm:h-64 lg:h-80"
```

---

## 💡 Полезные утилиты

### Line Clamp

Обрезка текста с многоточием:

```tsx
className="line-clamp-2"  // 2 строки
className="line-clamp-3"  // 3 строки
```

### Truncate

Обрезка длинного текста:

```tsx
className="truncate"  // Однострочное обрезание
```

### Whitespace

```tsx
className="whitespace-nowrap"  // Без переноса
className="break-words"        // С переносом слов
```

---

## 📱 Тестирование

### Chrome DevTools

1. Откройте DevTools (F12)
2. Включите Device Toolbar (Ctrl+Shift+M)
3. Выберите устройство или custom размер

### Рекомендуемые разрешения для теста:

- **320px** - iPhone SE (мин. ширина)
- **375px** - iPhone 12/13
- **768px** - iPad
- **1024px** - iPad Pro
- **1280px** - Desktop
- **1920px** - Full HD Desktop

---

## ✅ Чеклист мобильной адаптации

- [x] TopBar адаптирован
- [x] Header с бургер-меню
- [x] HeroSection масштабируется
- [x] ProgramBenefits - адаптивная сетка
- [x] WhoProgramFor - адаптивная сетка
- [x] CaseBasedLearning - изменение порядка
- [x] KeyBenefits - адаптивная диаграмма
- [x] Founder - изменение порядка
- [x] CTABanner - скрытие декора
- [x] Footer - адаптивный layout
- [x] Blog - адаптивные карточки
- [x] BlogPost - адаптивный контент
- [x] Мобильное меню работает
- [x] Изображения масштабируются
- [x] Тексты читабельны
- [x] Кнопки доступны для нажатия
- [x] Отступы корректны
- [x] Переполнения нет

---

## 🎯 Лучшие практики

### 1. Mobile-First подход

Начинайте с мобильных стилей, затем добавляйте для больших экранов:

```tsx
// ✅ Правильно
className="text-sm sm:text-base lg:text-lg"

// ❌ Неправильно
className="lg:text-lg md:text-base text-sm"
```

### 2. Touch-friendly элементы

Минимальный размер кликабельных элементов: **44px × 44px**

```tsx
// ✅ Хорошо
<button className="min-h-[44px] min-w-[44px] p-3">

// ❌ Слишком мало
<button className="p-1">
```

### 3. Читабельный текст

Минимальный размер шрифта: **14px (text-sm)**

```tsx
// ✅ Читабельно
className="text-sm sm:text-base"

// ❌ Слишком мелко
className="text-xs"
```

### 4. Оптимизация изображений

```tsx
// Адаптивные изображения
<img 
  srcSet="image-320.jpg 320w, image-768.jpg 768w, image-1280.jpg 1280w"
  sizes="(max-width: 768px) 100vw, 50vw"
/>
```

### 5. Sticky Header

```tsx
// Header прилипает к верху при скролле
<header className="sticky top-0 z-50">
```

---

## 🚀 Производительность

### Оптимизации для мобильных:

1. **Lazy loading изображений**
2. **Минимизация CSS**
3. **Code splitting**
4. **Responsive images**
5. **Reduced motion для анимаций**

---

## 📚 Ресурсы

- [Tailwind CSS Responsive Design](https://tailwindcss.com/docs/responsive-design)
- [MDN - Responsive Images](https://developer.mozilla.org/en-US/docs/Learn/HTML/Multimedia_and_embedding/Responsive_images)
- [Google - Mobile-Friendly Test](https://search.google.com/test/mobile-friendly)

---

**Готово!** Сайт полностью адаптирован для всех устройств! 📱✨

