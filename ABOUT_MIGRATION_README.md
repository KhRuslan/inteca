# About Page Migration

## 📄 Описание

Эта миграция добавляет контент страницы "О программе" (About) ко всем трем языкам в таблице `site_content`.

**⚠️ Важно:** Миграция **НЕ удаляет** существующие данные (Home page), а только **добавляет** новое поле `about`!

---

## 🚀 Как использовать

### Шаг 1: Убедитесь что базовая миграция выполнена

Сначала должна быть выполнена `multilingual-migration.sql` (Home page).

Проверьте в Supabase Table Editor → `site_content`:
- ✅ `site_content_ru` существует
- ✅ `site_content_en` существует
- ✅ `site_content_kz` существует

### Шаг 2: Выполните миграцию About

```sql
1. Откройте Supabase Dashboard → SQL Editor
2. Создайте New query
3. Откройте файл multilingual-migration-about.sql
4. Скопируйте ВЕСЬ код
5. Вставьте и нажмите RUN
```

⏱️ Выполнение: 1-2 секунды

### Шаг 3: Проверьте результат

В Supabase Table Editor → `site_content` → откройте любую запись:

В поле `data` теперь должно быть:
```json
{
  "hero": { ... },          // Home page (было)
  "programBenefits": [...], // Home page (было)
  "whoProgramFor": [...],   // Home page (было)
  "about": {                // About page (НОВОЕ!)
    "hero": { ... },
    "managementChallenges": [...],
    "flexibleFormats": [...],
    ...
  }
}
```

---

## 📊 Что добавляет миграция

### Для каждого языка (ru, en, kz) добавляется:

**`about.hero`**
- title (заголовок страницы)
- description (описание программы)
- formula (формула кейс-метода)
- image (путь к изображению)

**`about.managementChallenges`** (7 областей)
- Стратегия
- Финансы
- Маркетинг
- HR
- Продажи
- Лидерство
- Экономика

**`about.flexibleFormats`** (4 формата обучения)
- Корпоративные тренинги
- Модульные программы
- Интеграция в MBA
- Почему форматы эффективны

**`about.afterCompleting`**
- Инвестиции в образование (title, description, buttonText, image)

**`about.coreCompetencies`** (4 компетенции)
- Case Learning Path
- Думай глобально — действуй локально
- Практические навыки
- Личная экосистема компетенций

---

## 🔄 Как это работает

Миграция использует PostgreSQL оператор `||` (jsonb concatenation):

```sql
UPDATE site_content
SET data = data || '{ "about": { ... } }'::jsonb
WHERE key = 'site_content_ru';
```

Это **добавляет** новое поле `about` к существующему JSON, **не удаляя** другие поля.

---

## ✅ Проверка на сайте

После выполнения миграции:

1. Перезапустите dev сервер (если нужно):
```bash
npm run dev
```

2. Перейдите на страницу About: `http://localhost:5173/about`

3. Переключите языки (KZ / RU / EN)

4. Контент должен меняться на всех языках

---

## 🐛 Troubleshooting

### Ошибка: "column data does not exist"

**Причина:** Базовая миграция не выполнена.

**Решение:** Сначала выполните `multilingual-migration.sql`

### Ошибка: "syntax error near ||"

**Причина:** Не скопировали весь SQL код.

**Решение:** Убедитесь что скопировали **весь** файл от начала до конца.

### Контент не меняется на сайте

**Решение:**
1. Очистите кэш браузера
2. Перезапустите dev сервер
3. Проверьте в Supabase что поле `about` добавлено
4. Проверьте Console на ошибки

---

## 📝 Следующие шаги

После выполнения этой миграции можно:

1. ✅ Проверить страницу About на всех языках
2. 📝 Создать миграции для других страниц (Founder, Methodology и т.д.)
3. 🎨 Настроить контент через админку `/admin`

---

**Миграция готова к использованию!** 🎉

